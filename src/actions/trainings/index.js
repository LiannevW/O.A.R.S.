import fetchTrainings, { FETCHED_TRAININGS} from './fetch'
import fetchOneTraining, {FETCHED_ONE_TRAINING} from './fetch'


export {
  fetchTrainings,
  fetchOneTraining,
  FETCHED_TRAININGS,
  FETCHED_ONE_TRAINING,
}
